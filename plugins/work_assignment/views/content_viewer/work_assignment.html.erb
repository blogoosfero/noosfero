<% extend WorkAssignmentPlugin::Helper %>

<%= button :diagramo, _('Enviar diagrama'), profile.admin_url.merge(:controller => :cms, :action => :new, :type => 'DiagramoPlugin::Diagram', :parent_id => @page.id) %>

<% unless @page.body.blank? %>
  <div><%= @page.body %></div>
  <hr/>
<% end %>

<%= display_submissions(@page, user) %>

<%= javascript_include_tag '../plugins/work_assignment/show_versions' %>
